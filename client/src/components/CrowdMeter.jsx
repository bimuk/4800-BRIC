import React from 'react';
import { Typography } from '@mui/material';

const CrowdMeter = () => {
    return (
        <div>
            <Typography variant="h4">Crowd Meter</Typography>
            <Typography variant="body1">This is the Crowd Meter content.</Typography>
        </div>
    );
};

export default CrowdMeter;